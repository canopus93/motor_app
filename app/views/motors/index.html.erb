<div class="col-sm-12 text-center"><h3>Motor List</h3></div>

<table class="table">
	<thead>
		<tr>
			<th>Name</th>
			<th>Action</th>
		</tr>
	</thead>
	<tbody>
		<% @motors.each do |motor| %>
			<tr>
				<td><%= motor.link_to_show %></td>
				<td><%= motor.link_to_edit %> <%= motor.link_to_delete %>										
				</td>
			</tr>
		<% end %>
	</tbody>
	<tfoot>
		<tr>
			<td></td>
			<td><ul class="pull-right"><%= link_to 'New', new_motor_path, class: 'btn btn-primary btn-xs' %></ul></td>
		</tr>
	</tfoot>
</table>
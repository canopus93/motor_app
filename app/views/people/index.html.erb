<div class="col-sm-12 text-center"><h3>Person List</h3></div>

<table class="table">
	<thead>
		<tr>
			<th>Name</th>
			<th>Action</th>
		</tr>
	</thead>
	<tbody>
		<% @people.each do |person| %>
			<tr>
				<td><%= person.link_to_show %></td>
				<td><%= person.link_to_edit %> <%= person.link_to_delete %>										
				</td>
			</tr>
		<% end %>
	</tbody>
	<tfoot>
		<tr>
			<td></td>
			<td><ul class="pull-right"><%= link_to 'New', new_person_path, class: 'btn btn-primary btn-xs' %></ul></td>
		</tr>
	</tfoot>
</table>